[build]
  command = "yarn run build && cp build/* releases/"
  publish = "releases"
  functions = "functions"

  [build.environment]
    YARN_FLAGS = "--frozen-lockfile"
    YARN_VERSION = "1.22.4"
    NODE_VERSION = "12"

